<h1>Editing Product</h1>

<%= render 'form', product: @product %>

  <%= link_to 'Show', class: 'btn btn-default' @product %>,
  <%= link_to 'Back', products_path, class: 'btn btn-default back-btn' %>

